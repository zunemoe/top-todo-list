{
  "name": "webpack-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx webpack serve --open",
    "start:network": "npx webpack serve --host 0.0.0.0 --port 8080 --allowed-hosts all",
    "build": "npx webpack --mode=production",
    "build:dev": "npx webpack --mode=development",
    "deploy": "npm run build && npx gh-pages -d dist",
    "deploy:clean": "npm run build && npx gh-pages -d dist -f",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "zunemoe:webpack-template"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^7.1.2",
    "gh-pages": "^6.3.0",
    "html-loader": "^5.1.0",
    "html-webpack-plugin": "^5.6.3",
    "style-loader": "^4.0.0",
    "webpack": "^5.100.2",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.2.2"
  },
  "dependencies": {
    "air-datepicker": "^3.6.0",
    "date-fns": "^4.1.0"
  }
}
